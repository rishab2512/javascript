const prompt = require('prompt-sync')();
let string = prompt('enter a string: ');
let strLength = string.length;
let revstr = '';
for(let i = strLength-1 ; i>=0 ; i--){
    revstr += string[i];
}
console.log(`revrse string is ${revstr}`);
